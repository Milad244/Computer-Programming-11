import java.util.ArrayList;
import java.util.Scanner;
import java.util.Stack;

public class Main {
    public static void main(String args[]){
        /*
        Employees Milad = new Employees("Milad", true, 90000);
        System.out.println(Milad.isCool());
        System.out.println(Milad.getName());
        System.out.println(Milad.getIdNum());

        Employees James = new Employees("James", false, 5000);
        System.out.println(James.getSalary());
        System.out.println(James.getIdNum());

        James.fireEmployee();
        System.out.println(James.isFired());

        Employees Oliver = new Employees();
        System.out.println(Oliver.getIdNum());

        Oliver.increasePay(3000);
        System.out.println("Salary now is " + Oliver.getSalary());
        Oliver.increasePay(3000);
        System.out.println("Salary now is " + Oliver.getSalary());

        ArrayList<Employees> employees = new ArrayList<>();
        employees.add(new Employees());
        for (int i=0; i < 100; i++){
            employees.add(new Employees());
        }
        for (int i=0; i<employees.size(); i++){
            employees.get(i).setSalary((int)(Math.random()*10000));
            System.out.println(employees.get(i).getSalary());
        }
        System.out.println("HERE");
        Object [] scuffedEmployees = employees.toArray();
        System.out.println(scuffedEmployees[5]);
        Employees badEmployee = (Employees) scuffedEmployees[5];
        System.out.println(badEmployee.getSalary());

        Employees [] unscuffedEmployees = employees.toArray(new Employees[0]);
        Employees goodEmployee = unscuffedEmployees[6];
        System.out.println(goodEmployee.getSalary());
        System.out.println(unscuffedEmployees[7].getSalary());

        System.out.println(Milad);
        Employees MiladClone = new Employees("Milad", false, 0);
        System.out.println(Milad.equals(MiladClone));

        ArrayList<Integer> numberedList = new ArrayList<>();
        numberedList.add(5);
        System.out.println(numberedList.getFirst());

        Scanner scan = new Scanner(System.in);
        int age = 0;
        boolean isValid = false;
        while (!isValid){
            try {
                System.out.println("Enter your age: ");
                age = scan.nextInt();
                if (age >= 0 && age <= 150){
                    isValid = true;
                }
            } catch(Exception e){
                System.out.println("Incorrect input, please try again");
                scan.next();
            }
        }
        System.out.println("NICE JOB! Your age is " + age);
        */
        System.out.println("HI");

    }
}